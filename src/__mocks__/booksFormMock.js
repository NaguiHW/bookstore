const booksFormMock = {
  handleChange: jest.fn(),
  handleSubmit: jest.fn(),
  categories: ['Action', 'Biography', 'History', 'Horror', 'Kids', 'Learning', 'Sci-Fi'],
};

export default booksFormMock;
